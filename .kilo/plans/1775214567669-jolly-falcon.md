# Sidebar Redesign Plan

## Goal

Match the sidebar design with: (1) logo at top, (2) compact/filled menu items with minimal spacing.

## Changes Required

### 1. AppSidebar.tsx

Add a logo image at the top of the sidebar, above the menu items.

- **Logo URL**: `https://5geycduwue.ufs.sh/f/KIddkrDeaHXJ6pCA7FljdVZrCkXFEDWYa8bpU9fB647TP5LI`
- **Implementation**: Use Next.js `Image` component with the external URL
- **Logo placement**: At the very top of `SidebarContent`, before `SidebarGroup`
- **Suggested styling**:
  - `width={120}` or `height={40}` with `object-contain`
  - `className="mx-auto py-4"` for centering and vertical spacing

### 2. SidebarAccordion.tsx

Increase icon height, margin, and padding for better readability.

- **Current**: `p-3` padding, `size-5` icons
- **Change**:
  - Padding: `p-3` → `p-4` (16px)
  - Margin: Add `my-1` for vertical spacing between items
  - Icon size: `size-5` (20px) → `size-6` (24px)
- **Goal**: Less cramped, more balanced appearance

## Implementation Steps

1. **AppSidebar.tsx**: Add logo at top

   ```tsx
   <SidebarContent>
     <div className="px-3 py-4">
       <Image
         src="https://5geycduwue.ufs.sh/f/KIddkrDeaHXJ6pCA7FljdVZrCkXFEDWYa8bpU9fB647TP5LI"
         alt="Logo"
         width={120}
         height={40}
         className="mx-auto object-contain"
       />
     </div>
     <SidebarGroup>
       ...
   ```

2. **SidebarAccordion.tsx**: Increase spacing
   - In the `TeacherMenuItem` component, update the `SidebarMenuButton`:
     - Change `className={cn('p-3', ...)}` to `className={cn('p-4 my-1', ...)}`
     - Change `<Icon className="size-5" />` to `<Icon className="size-6" />`

## Notes

- Next.js `Image` component requires domain configuration for external URLs in `next.config.js`
- May need to add `5geycduwue.ufs.sh` to the `images.domains` array
