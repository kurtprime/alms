# Plan: Add Teacher Sidebar Menu Items

## Goal

Add Dashboard, My Classes, Resources, Activities, Gradebook, Students, Announcements, and Archived Classes to the teacher's sidebar.

## Current State

- Teacher sidebar is defined in `src/modules/user/ui/components/Teacher/SidebarAccordion.tsx`
- Currently has: "To Check", "Classes" (with hover card), and "AI Features"
- Route group is `src/app/(teacher)/`

## Implementation Steps

### Step 1: Update SidebarAccordion.tsx

Add 8 new `SidebarMenuItem` components with `SidebarMenuButton` for each menu item:

- Dashboard (`/teacher`)
- My Classes (`/teacher/classes`)
- Resources (`/teacher/resources`)
- Activities (`/teacher/activities`)
- Gradebook (`/teacher/gradebook`)
- Students (`/teacher/students`)
- Announcements (`/teacher/announcements`)
- Archived Classes (`/teacher/archived`)

### Step 2: Add Icons

Import appropriate lucide-react icons for each item (e.g., LayoutDashboard, BookOpen, FileText, ClipboardList, Grade, Users, Bell, Archive).

### Step 3: Create Page Routes

Create placeholder pages in `src/app/(teacher)/`:

- `teacher/page.tsx` - Dashboard
- `classes/page.tsx` - My Classes
- `resources/page.tsx` - Resources
- `activities/page.tsx` - Activities
- `gradebook/page.tsx` - Gradebook
- `students/page.tsx` - Students
- `announcements/page.tsx` - Announcements
- `archived/page.tsx` - Archived Classes

## Decisions Made

1. **Placement**: After existing items (To Check, Classes, AI Features)
2. **Style**: Direct links (not hover cards)
3. **Active highlighting**: Yes - use `useIsActivePath` hook to highlight current page

## Implementation Steps

### Step 1: Update SidebarAccordion.tsx

Add 8 new `SidebarMenuItem` components with `SidebarMenuButton` for each menu item. Use `useIsActivePath` hook for active state styling (matching pattern in `SidebarMenuItemsParams.tsx`).

Menu items (in order):
| Title | URL | Icon |
|-------|-----|------|
| Dashboard | `/teacher` | LayoutDashboard |
| My Classes | `/teacher/classes` | BookOpen |
| Resources | `/teacher/resources` | FileText |
| Activities | `/teacher/activities` | ClipboardList |
| Gradebook | `/teacher/gradebook` | Grade |
| Students | `/teacher/students` | Users |
| Announcements | `/teacher/announcements` | Bell |
| Archived Classes | `/teacher/archived` | Archive |

### Step 2: Create Page Routes

Create placeholder pages in `src/app/(teacher)/`:

- `teacher/page.tsx` - Dashboard
- `classes/page.tsx` - My Classes
- `resources/page.tsx` - Resources
- `activities/page.tsx` - Activities
- `gradebook/page.tsx` - Gradebook
- `students/page.tsx` - Students
- `announcements/page.tsx` - Announcements
- `archived/page.tsx` - Archived Classes

Each page should have a basic structure with a heading matching the menu title.
