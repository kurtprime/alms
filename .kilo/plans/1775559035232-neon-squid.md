# Plan: TabsList Background & Border Radius

## Changes to `src/modules/user/ui/Views/ClassIdView.tsx`

### 1. Background color with opacity

- Add `bg-sidebar/60` to TabsList className
- This uses the existing `--sidebar` CSS variable (`oklch(0.2178 0 0)` in dark mode) at 60% opacity
- Remove `md:bg-transparent` since the sidebar now has an intentional background

### 2. Rounded right corners (desktop only)

- Replace `md:rounded-none` with `md:rounded-r-lg`
- Left side stays flat against the viewport edge; top-right and bottom-right corners get rounded
- Mobile remains unrounded since it's full-width horizontal

### Final TabsList className

```
h-12 w-full justify-start rounded-none bg-sidebar/60 p-6 md:flex-col md:h-auto md:w-56 md:justify-start md:rounded-r-lg md:bg-sidebar/60 md:p-6
```
